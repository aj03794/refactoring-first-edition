const setValue = (name, value) => {
    if (name.equals("height")) {
        return height = value
    }
    if (name.equals("width")) {
        return width = value
    }
}

// ---------------------------

const setHeight = (arg) => {
    height = arg
}

const setWidth = (arg) => {
    width = arg
}
